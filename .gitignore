# Backups / duplicates (index.html is the deployed file)
amira-hephzibah-final.backup.html
amira-hephzibah-final.html

# Loose source videos in the project root are ignored,
# but the friend videos served by the site (videos/) are tracked.
*.mp4
!videos/*.mp4

.vercel
